# Amber Electric API Configuration
AMBER_API_KEY=psk_your_api_key_here
AMBER_SITE_ID=

# Monitoring Configuration
# Feed-in price threshold in cents per kWh (c/kWh)
# When feed-in price exceeds this value, you'll get a notification
FEED_IN_THRESHOLD=15.0

# How often to check prices
# Second-based: 5s, 15s, 30s (for testing/fast updates)
# Minute-based (cron): */1 * * * *, */5 * * * *, */15 * * * *, */30 * * * *
# Examples:
#   5s = every 5 seconds (testing)
#   15s = every 15 seconds
#   30s = every 30 seconds
#   */1 * * * * = every 1 minute
#   */5 * * * * = every 5 minutes (recommended)
CHECK_INTERVAL=*/5 * * * *

# Timezone for scheduling (Australia timezones)
# Options: Australia/Sydney, Australia/Melbourne, Australia/Brisbane,
#          Australia/Adelaide, Australia/Perth
TIMEZONE=Australia/Sydney

# Notification Configuration
# Channels: console, desktop (comma-separated)
NOTIFICATION_CHANNELS=console,desktop

# Logging Configuration
# Levels: trace, debug, info, warn, error
LOG_LEVEL=info

# Pretty print logs (useful for development, set false for production)
LOG_PRETTY=true
